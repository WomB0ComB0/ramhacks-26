{
  "name": "ramhacks-26",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "concurrently -n backend,frontend -c blue,magenta \"npm run build --prefix backend\" \"npm run build --prefix frontend\"",
    "start": "concurrently -n backend,frontend -c blue,magenta \"npm run start --prefix backend\" \"npm run preview --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  }
}
