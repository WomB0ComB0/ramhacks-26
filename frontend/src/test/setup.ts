// Vitest global setup. Extends `expect` with @testing-library/jest-dom
// matchers (toBeInTheDocument, toHaveTextContent, etc.) so tests can use
// the same assertion vocabulary as the wider React testing ecosystem.

import "@testing-library/jest-dom/vitest";
